== Utility_RandomPassword_Generate_V1

Generates a human readable random password - intended to be immediately changed
once used.

The random password that is generated is derived from a 4 or 5 letter word with
a 4 digit number appended to the word.  The word is randomly selected from a
list of 4 or 5 letter words defined in the init.rb.  The number is randomly
selected in a range from 1000 to 8999.  The resulting password is the
combination of the random word and the random number, it ranges from 8 to 9
characters.

=== Results
[temporary_password]
  The random password generated by the handler.